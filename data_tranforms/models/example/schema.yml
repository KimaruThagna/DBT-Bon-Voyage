
version: 2

models:
    - name: custom__store_administation_county
      description: '{{doc("model_description")}}'
      columns:
          - name: county_data.county_code
            description: "County code"
            tests:
                - unique
                - not_null
                - is_kajiado

    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
